services:
  - type: web
    name: hotel-new-kishan
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
      - key: IS_RENDER
        value: "true"
